(define red "red")
(define green "green")
(define blue "blue")

(define triangle "triangle")
(define square "square")
(define circle "circle")

(define t (lambda (color) (shape triangle color)))
(define s (lambda (color) (shape square color)))
(define c (lambda (color) (shape circle color)))
