;standard loscript library with helper stuff for skomakare API

(define pi 3.14159265359)

(define red "red")
(define green "green")
(define blue "blue")

(define triangle (lambda (color) (shape "triangle" color)))
(define square (lambda (color) (shape "square" color)))
(define triangle-fan (lambda (color) (shape "triangle-fan" color)))

(define test-loop (lambda () (do (print "got here ok") (sleep 2))))
